import { SectionJuiceAgent } from "../../agents/practice/SectionJuiceAgent";
import { JuiceChallenge } from "./model/JuiceChallenge";

export class ChallengeFactory {

    // static juiceChallenge(data: typeof SectionJuiceAgent.outputSchema): JuiceChallenge {

    //     return new JuiceChallenge({
    //         topicId: data.topicId,
    //         topicCode: data.topicCode,
    //         context: "", // Context can be filled later if needed
    //         toRemember: data.juice.map(item => ({
    //             toRemember: item.toRemember,
    //             date: item.date ? {
    //                 day: item.date.day,
    //                 month: item.date.month,
    //                 year: item.date.year
    //             } : null
    //         }))
    //     });
    // }
}